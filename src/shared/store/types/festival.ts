export interface interestFestival {
  imgSrc: string;
  schoolName: string;
  festivalName: string;
  date: string;
  festivalId: string;
  schoolId: string;
  lat: number;
  lng: number;
}
export interface interestFestivalArrData {
  data: interestFestival[];
}
